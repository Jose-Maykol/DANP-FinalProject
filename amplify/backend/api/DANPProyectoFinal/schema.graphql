type IoTData @model @auth(rules: [{allow: public}]) {
  id: ID!
  datetime: String
  humidity: String
  temperature: String
}

type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  nombres: String
  apellidos: String
  email: String
  password: String
}
 